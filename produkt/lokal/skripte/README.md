# Hilfsskripte für die lokale Installation

Aufruf:

```
docker compose run --rm wpcli eval-file /opt/skripte/<datei>.php [argumente]
```

| Datei | Zweck |
|---|---|
| `variante-aktivieren.php` | Style Variation aus `styles/<name>.json` anwenden, ohne den Site-Editor |
